<% content_for :header do %>
<section class="hero is-primary is-bold">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        Explorar los instrumentos
      </h1>
    </div>
  </div>
</section>
<% end %>

<div class="instrument-index-grid pt4">
  <% @instruments.each do |instrument| %>

    <div class="instrument border-light">
      <div class="instrument-thumb">
      <%= link_to image_tag(instrument.image_url(:thumb)), instrument %>

      <% if instrument.condicion? %>
        <div class="condicion">
          <span class="tag is-dark"><%= instrument.condicion %></span>
        </div>
      <% end %>
      </div>


    <div class="pa3">

      <h3 class="fw7 f4 title"><%= link_to instrument.titulo, instrument %></h3>

      <p class="has-text-gray fg pt1">Vendido por <%= instrument.user.name %></p>

      <p class="f3 fw6 has-text-right pt2 price"><%= number_to_currency(instrument.precio) %></p>

      <% if instrument_author(instrument) %>
         <%= link_to 'Editar', edit_instrument_path(instrument), class: "button is-small" %>
         <%= link_to 'Eliminar', instrument, method: :delete, data: { confirm: "Estas seguro ?" }, class: "button is-small" %>
      <% end %>

    </div>
  </div>
  <% end %>
</div>